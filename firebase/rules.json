{
  "rules": {
    "rooms": {
      ".read": true,
    },
    "questions": {
      "$room": {
        ".write": "!data.exists() && auth != null"
      }
    },
    "votes": {
      "$userid": {
        ".write": "$userid === auth.uid"
      }
    }
  }
}
